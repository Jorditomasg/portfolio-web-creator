# Portfolio Web Creator - Environment Configuration
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your values
# 3. Run: docker-compose up -d

# ===========================================
# REQUIRED - Set secure values!
# ===========================================

# Database password (change this!)
POSTGRES_PASSWORD=your_secure_password_here

# JWT secret for auth (generate with: openssl rand -hex 32)
JWT_SECRET=your_random_jwt_secret_here

# ===========================================
# OPTIONAL - Customization
# ===========================================

# Admin credentials (for first login)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Database (defaults work fine)
POSTGRES_USER=portfolio_user
POSTGRES_DB=portfolio_db

# Port to expose (default: 80)
PORT=80

# Custom Docker image (if using your own)
# DOCKER_IMAGE=your-username/portfolio-app:latest
